const objEagle = window.document.querySelector('#eagle');

class objEagle{
  constructor(posX,posY){
    this.posX = posX,
    this.posY = posY,
    this.carColorCords = ['-160px -127px','-211px -245px','-160px -368px',]/* saraxtit visas iespejas color  */
  }

}

const renderObj = (screenHeight,objHeight,screenWidth,objWidth) =>{
  let fallFrom = screenHeight - obsjHeight;
  const randNr = () => Math.floor(Math.random())
  let eagle = new objEagle(Math.random,0);

}

setTimeout(()=>{
  
},3000)